# Claude Code local settings (machine-specific permission overrides)
.claude/settings.local.json

# BAT scripts

# VM disk images and archives — large binaries, never committed
*.vhd
*.vhdx
*.vhd.tar.gz
*.iso

# Sentinel files written by provision.ps1 alongside cached base images
*.nocloud-patched

# Extraction scratch directories created during base image download
_extract_*/

# Pester test result report - generated by Run-Tests.ps1, not source
TestResults.xml

# graphify knowledge graph artifacts (local; rebuilt by graphify)
graphify-out/
supersets-out/

